import React from 'react';
import NavBar from './NavBar/NavBar';

const Header: React.FC = () => {
    return (
        <header >
            <NavBar />
        </header>
    );
};

export default Header;
